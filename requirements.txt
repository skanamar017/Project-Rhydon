# Flask and Web Framework Dependencies
Flask==2.3.3
flask-cors==4.0.0

# Data Validation and Models
pydantic==2.4.2

# HTTP Requests for PokeAPI integration
requests==2.31.0

# Data handling (compatible with streamlit if installed)
pandas>=1.5.0,<2.0.0

# Python Standard Library (included by default, listed for reference)
# sqlite3 - built-in SQLite database interface
# json - built-in JSON handling
# typing - built-in type hints
# os - built-in operating system interface
# time - built-in time utilities
# subprocess - built-in subprocess management
# sys - built-in system-specific parameters
# random - built-in random number generation
# math - built-in mathematical functions
# pathlib - built-in object-oriented filesystem paths
# shutil - built-in high-level file operations

# Development and Testing (optional)
pytest==7.4.2
pytest-flask==1.2.0

# Production WSGI Server (optional)
gunicorn==21.2.0
